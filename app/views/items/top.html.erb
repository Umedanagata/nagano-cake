<head>
  <style media="screen">
    .welcome-message {
      border: 3px solid gray;
      width: 600px;
    }
    .item-box {
      float: left;
      margin-right: 10px;
      margin-top: 30px;
    }
  </style>
</head>

<div class="container">
  <div class="row">
    <div class="col-xs-2 col-xs-offset-1">
      <%= render "search_form" %>
    </div>
    <div class="col-xs-9">
      <div class="welcome-message", style="margin-top :50px;">
        <p style="text-align: center;">ようこそ、NaganoCakeへ!<br>
        このサイトは、ケーキ販売のECサイトになります。<br>
        会員でない方も商品の閲覧はできますが、
        購入には会員登録が必要になります。</p>
      </div>
      <div class="recommend-item">
        <h3>おすすめ商品</h3>

        <div class="item-index">
          <% @items.each.with_index(1) do |item, i| %>
            <div class="item-box">
              <% if item.is_sale == false%>
              <% else %>
              <div class="item-image">
                <%= i %>
                <%= link_to item_path(item) do %>
                <%= attachment_image_tag item, :image, :fill, 200, 150,
                  fallback: "noimage.jpg" %>
                <% end %>
              </div>
              <h4><%= item.name %></h4>
              <p>¥<%= (item.price * 1.08).floor %>(税込)</p>
              <% end %>
            </div>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</div>
