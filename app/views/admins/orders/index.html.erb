<h2>注文一覧画面</h2>

<!-- 注文一覧表示 -->
<table class="#">
    <tr>
        <th>購入日時</th>
        <th>購入者</th>
        <th>注文個数</th>
        <th>注文ステータス</th>
    </tr>
<% @customers.each do |customer| %>
    <tr>
        <td><%= customer.created_at%></td>
        <td><%= customer.created_at %></td>
        <td><%= customer.created_at %></td>
        <td><%= customer.created_at %></td>
    </tr>
</table>
<!-- ページャ実装 -->