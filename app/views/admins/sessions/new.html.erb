<div class="col-xs-2">
</div>

<div class="text-center">
  <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
    <div class="">
      <sapn>メールアドレス</sapn><%= f.email_field :email, autofocus: true, autocomplete: "email" %>
    </div><br><br>

    <div class="field">
      <span>パスワード</span>
      <%= f.password_field :password, autocomplete: "current-password" %>
    </div><br><br><br><br>

    <div class="actions">
      <%= f.submit "ログイン", class:"btn btn-primary , btn-lg" %>
    </div>
  <% end %>
</div>

<div class="col-xs-2">
</div>

