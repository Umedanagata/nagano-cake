<div class="conteiner">
  <div class="row">
    <div class="col-xs-3 col-xs-offset-1">
      <h2>ジャンル検策</h2>
      <h3>ケーキ</h3>
      <h3>焼き菓子</h3>
      <h3>プリン</h3>
      <h3>ケーキ</h3>
    </div>
    <div class="col-xs-8">
      <h1>商品一覧</h1><span>(全<%= @items.count %>件)</span>
      <% @items.each do |item| %>
        <div class="items-image">
          <%= attachment_image_tag %>
        </div>
        <h3></h3><%= link_to item.name, items_path(item) %>
        <p>¥ <%= item.price %></p>
      <% end %>
    </div>
  </div>
</div>
